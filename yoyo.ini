[DEFAULT]
# Direct yoyo CLI usage requires an already percent-encoded DATABASE_URL.
# Normal application deployments should use `python -m splash.migrate`.
database = %(DATABASE_URL)s
sources = migrations
batch_mode = off
verbosity = 0
